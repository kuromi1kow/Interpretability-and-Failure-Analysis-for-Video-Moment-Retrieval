# Narrative Plan

Audience: TA, professor, and classmates reviewing the final project presentation.

Objective: keep the original slide flow, but add one visual slide for each of the three analysis experiments so the audience sees concrete evidence instead of only tables and text.

Approach:

- Preserve the existing deck and create an updated copy.
- Insert one new slide after the temporal-bias section with:
  - the short-vs-medium-vs-long performance chart
  - failure-case timelines
- Insert one new slide after the verb-vs-noun section with:
  - the ablation metric chart
  - query examples showing tagged verbs and nouns
- Insert one new slide after the head-ablation section with:
  - the 32-head heatmap
  - a compact “most impactful heads” bar chart

Design intent:

- Match the current deck's simple academic style.
- Keep all new slides editable.
- Use the generated PNG figures as fixed visual assets.
- Add only short speaker-friendly captions, not long paragraphs.

Editability plan:

- Import the existing PPTX.
- Insert new slides near the matching experiment content instead of rebuilding the full deck.
- Export a new PPTX copy so the original deck stays untouched.
